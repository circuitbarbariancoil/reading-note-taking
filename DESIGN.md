# 日语纸书阅读伴侣 —— 设计文档 (v0.1)

> 个人工具。目标：消灭「读日语纸书时摘抄/高亮难以即时保存整理」的痛点。
> App 是数据的**唯一来源**，单向流向 Obsidian。Obsidian 只读渲染，不反向回写 App。

---

## 0. 一句话定位

> 一个长在 Dropbox 上、专门处理日语纸书页的「Markdown 高亮/摘抄采集端」。
> 拍一页 → 竖排 OCR 出冻结原文 → 在原文上高亮/摘抄成「条目」（带批注、#tag、回链）→ 同步 Dropbox → Obsidian 插件外链渲染。卡片/复习等深加工留给 Obsidian。

---

## 1. 核心概念与数据模型

### 1.1 三层

```
书 (Book)
 └─ 页 (Page)          原文冻结、只读、不可编辑
      ├─ OCR高亮 (PageHighlight)   直接在原文上做的高亮 → 渲染在原文上（展示层）
      └─ 条目 (Entry)             高亮/摘抄萃取出的独立副本 → 可编辑、可再高亮、批注、#tag
```

- **原文 (OCR text)**：LLM 输出的该页全文，**冻结**。要改错字只能在条目里改。
- **OCR高亮**：在原文上直接划的色块，存成「冻结文本上的偏移范围」，App 与插件都按它给原文上色。**不写进原文本身**（原文保持纯文本）。无逆向投影：条目之后怎么变，不回写原文。
- **条目**：一次高亮/摘抄动作生成一条独立副本。范围=用户选区（可单词、可跨句，不自动扩句）。条目可编辑、可在内部再用 `~={color}=~` 高亮、加批注、加 #tag，并保留 →原页链接。

### 1.2 偏移规范（必须全链路统一）

- 偏移单位 = **Unicode 码点 (code point)**，不是字节、不是 UTF-16 码元。
- App(Kotlin/JS) 与 Obsidian 插件(JS) 取子串时一律按码点换算，避免生僻汉字/emoji（代理对）错位。
- OCR高亮锚在**冻结原文**上 → 偏移恒稳，无需自愈/模糊重定位。

### 1.3 颜色规范（App 与 Obsidian 必须一致）

- 高亮语法沿用现有 Obsidian 插件：`~={颜色名}文字=~`。
- 颜色定义（名→色值）存为 Dropbox 上的**共享配置** `config/highlight-colors.json`，App 与插件都读它，保证两端渲染一致。
- 支持 Clean Mode：隐藏高亮标记，显示纯文本。

---

## 2. Dropbox 文件结构

```
/ReadingVault/
  config/
    highlight-colors.json          # 颜色名→色值，App 与插件共享
  books/
    <book_uid>/
      book.json                    # 元数据 + 页原文 + OCR高亮 + 条目（App 拥有，单向）
      pages/
        p0091_archive.webp         # 高清存档图（人校对用）
        p0091_ocr.jpg              # 768px 处理图（喂 LLM，可选保留）
        p0092_archive.webp
```

> 一本书一个 `book.json` 聚合所有结构化数据；页图单独放 `pages/`。

---

## 3. 数据结构 (`book.json`)

```jsonc
{
  "schema": 1,
  "uid": "01HX...",                  // 稳定 ID（ULID）
  "title": "金閣寺",
  "author": "三島由紀夫",
  "isbn": "9784101050089",
  "cover_url": null,                 // openBD 封面（可选）
  "created_at": "2026-06-29T19:00:00Z",
  "updated_at": "2026-06-29T19:30:00Z",
  "dropbox_root": "/ReadingVault/books/01HX...",

  "pages": [
    {
      "page": 91,                    // 书内页码（用户可改）
      "archive_image": "pages/p0091_archive.webp",
      "ocr_text": "月のような遠い乳房には…",   // 冻结全文
      "ocr_model": "gemini-3-flash-preview",
      "ocr_captured_at": "2026-06-29T19:05:00Z",
      "proofread": false,            // 是否已人工校对
      "highlights": [                // 直接在原文上的高亮（展示层）
        { "id": "h1", "start": 134, "end": 141, "color": "red" }
      ]
    }
  ],

  "entries": [                       // 萃取出的条目（你的卡片素材）
    {
      "id": "01HY...",
      "page": 91,
      "src_start": 134,              // 萃取时在原文中的码点偏移
      "src_end": 152,
      "text": "影谷蛇之との戦闘から二週間後",   // 副本，可编辑（改错字、加~={}=~）
      "kind": "highlight",           // "highlight" | "excerpt"
      "annotation": "ここの時間跳躍が…",        // 批注，可空
      "tags": ["日语/词汇", "PA?"],   // 行内 #tag 列表
      "created_at": "2026-06-29T19:06:00Z",
      "updated_at": "2026-06-29T19:06:00Z"
    }
  ]
}
```

要点：
- `pages[].highlights` = OCR高亮（锚冻结原文，按偏移渲染色）。
- `entries[]` = 独立条目，`text` 是萃取时的副本，**之后独立演化**，不回写原文。
- `entries[].src_start/end` 仅作「回链定位」用途（点条目能跳回原页那段），因为原文冻结，所以稳。
- `highlight` 与 `excerpt` 都是条目；区别仅 `kind`（高亮顺带在 `pages.highlights` 留一条展示用色块，摘抄不强制留色块）。

---

## 4. 同步行为（单向）

| 文件 | 写者 | 读者 |
|---|---|---|
| `book.json` | **仅 App** | App、Obsidian 插件（只读） |
| `pages/*` 图 | **仅 App** | App、插件（外链渲染） |
| `config/highlight-colors.json` | App（或手动） | App、插件 |
| vault 内你的素材卡片 | **仅你/Obsidian** | App 不碰 |

- App→Dropbox→Obsidian 单向。插件**绝不回写** `book.json`。
- Obsidian 里你后续做的深加工（升级成素材卡片/cloze）写在 **vault 自己的笔记**里，App 永不覆盖 → 冲突归零。
- 同步通道：Dropbox（App 用 Dropbox SDK 上传；Obsidian 插件用 Dropbox token 读取 + 换临时直链显示图）。
- 离线：拍照入本地队列，联网后补传；上传+落库成功后才删本地原始照片。

---

## 5. OCR 管线（已验证）

- 模型：`gemini-3-flash-preview`。
- 喂图：长边 **768px / JPEG q85**（精度=原图，体积↓60%）。另存高清 webp 供人校对。
- 严格 prompt：忠实全文、保留旧字体、**不臆造 ルvi**、不确定标 `〓`、分离页眉页码。
- 延迟约 13–30s/页，由输出生成主导 → **拍照与加工解耦，后台批量 OCR**。
- 已知风险：生僻汉字偶有替换 + 幻觉 ルビ → 故页图永久留存 + 条目可改。

---

## 6. 屏幕线框（文字版）

### 6.1 书架 Shelf
```
┌─────────────────────────────┐
│  我的书架            [+ 加书] │
│ ┌────┐ ┌────┐ ┌────┐        │
│ │封面│ │封面│ │封面│        │   封面墙（openBD 封面）
│ │金閣│ │…  │ │…  │        │   点封面 → 进书详情/页列表
│ └────┘ └────┘ └────┘        │
└─────────────────────────────┘
[+ 加书] → ISBN 扫码 / 手动填
```

### 6.2 拍照采集 Capture
```
┌─────────────────────────────┐
│  金閣寺            页码: 91 ▲▼│
│ ┌─────────────────────────┐ │
│ │     相机取景 + 自动裁边   │ │   ML Kit 文档扫描，自动矫正
│ └─────────────────────────┘ │
│        ( 拍 )  连拍N页        │   拍完即入队，继续读，不等 OCR
│  队列: ●OCR中 ●OCR中 ✓好了   │   后台批量 OCR
└─────────────────────────────┘
```

### 6.3 页加工台 Page Workbench（核心）
```
┌─────────────────────────────┐
│  金閣寺 p91   [未校对] [Clean]│
│ ┌─────────────────────────┐ │
│ │   页图（可放大核字）       │ │   上：高清存档图
│ └─────────────────────────┘ │
│ ─────────────────────────── │
│  月のような遠い乳房には、    │   下：OCR 原文（冻结）
│  すでに ~={red}柏木の手=~ が │   已高亮处显色
│  触れ、…                    │
│                             │
│  [选中文字] → 浮动条:        │   拖选 → 操作条
│   [赤][青][緑]  [摘抄] [查词] │   点色=秒存高亮+生成条目
│                             │
└─────────────────────────────┘
[摘抄] → 弹「条目」面板（见 6.4）
```

### 6.4 条目编辑面板 Entry（底部弹出）
```
┌─────────────────────────────┐
│  条目                  [存]  │
│ 正文（可改错字/再高亮）:      │
│  ┌─────────────────────────┐│
│  │影谷蛇之との戦闘から二週間後││
│  └─────────────────────────┘│
│ 批注:                        │
│  ┌─────────────────────────┐│
│  │                         ││
│  └─────────────────────────┘│
│ 标签: #日语/词汇 #PA?  [+]   │   复用现有标签树自动补全
│ 来源: 金閣寺 p91  →看原页    │   自动带，不用手填
└─────────────────────────────┘
```

### 6.5 本书条目集 Entry List（汇总）
```
┌─────────────────────────────┐
│  金閣寺 · 条目 (68)  [搜索] [↕]│
│  按页/日期/标签 分组          │
│ ┌─────────────────────────┐ │
│ │ p91 影谷蛇之との戦闘から  │ │   不翻页浏览本书所有条目
│ │ #日语/词汇  2026-06-29   │ │   点击 → 跳回原页 / 编辑
│ ├─────────────────────────┤ │
│ │ p92 …                   │ │
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

---

## 7. Obsidian 插件行为

- 读 Dropbox 上 `book.json`（token 持有），**只读**。
- 渲染：
  - 一本书一个视图/笔记：书档案 + 各页原文（按偏移上 OCR高亮色）+ 条目列表。
  - 图片：用 token 换 Dropbox **临时直链** `<img>`，不下载进 vault。
  - 高亮色读 `config/highlight-colors.json`，与 App 一致。
- 提供**插件内全文搜索**（搜 `book.json` 的 ocr_text/条目），弥补「原文不进 vault → Obsidian 原生搜索看不到」。
- 你后续把值得的条目升级进自己的素材卡片库 —— 那是 vault 内手工动作，与本插件/App 无关。

---

## 8. 待定 / 后续

- 技术栈：安卓（Kotlin 原生 / Flutter / RN）、Markdown 编辑器库、Dropbox SDK、ML Kit。
- 重叠/嵌套高亮的叠色渲染规则。
- OCR「低置信字」标注（`〓`）在 UI 上的提示方式。
- 颜色配置的初始来源（从现有 Obsidian 插件导出对齐）。
```
```
